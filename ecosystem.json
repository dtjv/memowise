{
  /**
   * Application configuration section
   * http://pm2.keymetrics.io/docs/usage/application-declaration/
   */
  apps : [
    {
      name      : "server",
      script    : "./dist/server/server.js",
      env_production : {
        NODE_ENV: "production",
        NODE_PORT: 80,
      }
    },
  ],

  /**
   * Deployment section
   * http://pm2.keymetrics.io/docs/usage/deployment/
   */
  deploy : {
    production : {
      user : "root",
      host : "104.131.159.249",
      ref  : "origin/feat/dist-server-run-bg",
      repo : "git@github.com:mvargeson/wonky-mongoose.git",
      path : "/var/www/production",
      "post-deploy" : "npm install && npm run build:dist && pm2 startOrRestart ecosystem.json --env production",
    }
  }
}
